# javascript-lotto-precourse

---

## 기능 요구 사항

간단한 로또 발매기를 구현한다.

- [x] 1개의 로또를 발행할 때 중복되지 않는 6개의 숫자를 뽑는다.
  - [x] 로또 번호의 숫자 범위는 1~45까지이다.
- [x] 당첨 번호 추첨 시 중복되지 않는 숫자 6개와 보너스 번호 1개를 뽑는다.
- [x] 당첨은 1등부터 5등까지 있으며, 당첨 기준과 금액에 부합해야 한다.
- [x] 로또 구입 금액을 입력하면 구입 금액에 해당하는 만큼 로또를 발행해야 하며, 로또 1장의 가격은 1,000원이다.
- [x] 당첨 번호와 보너스 번호를 입력받은 후 -> 사용자가 구매한 로또 번호와 당첨 번호를 비교하여 당첨 내역 및 수익률을 출력하고 로또 게임을 종료한다.
- [x] 사용자가 잘못된 값을 입력할 경우 "[ERROR]"로 시작하는 메시지와 함께 Error를 발생시키고 **해당 메시지를 출력한 다음 해당 지점부터 다시 입력을 받는다.**

---

## 과제 진행 요구 사항

- [x] package.json 파일은 변경할 수 없으며, 제공된 라이브러리와 스타일 라이브러리 이외의 외부 라이브러리는 사용하지 않는다.
- [ ] 자바스크립트 코드 컨벤션을 지키면서 프로그래밍한다.
- [ ] indent(인덴트, 들여쓰기) depth를 3이 넘지 않도록 구현한다. 2까지만 허용한다.
- [x] 3항 연산자를 쓰지 않는다.
- [ ] 함수(또는 메서드)의 길이가 15라인을 넘어가지 않도록 구현한다. → 함수(또는 메서드)가 한 가지 일만 잘 하도록 구현한다.
- [x] Jest를 이용하여 정리한 기능 목록이 정상적으로 작동하는지 테스트 코드로 확인한다.
- [x] else를 지양한다. → 어느 경우에 쓰는 것이 적절한지 스스로 고민해본다.
- [x] 구현한 기능에 대한 단위 테스트를 작성한다. 단, UI(System.out, System.in, Scanner) 로직은 제외한다.
- [x] @woowacourse/mission-utils에서 제공하는 Random 및 Console API를 사용하여 구현해야 한다.
- [x] Lotto 클래스는 제공된 Lotto 클래스를 사용하여 구현해야 한다.

---

## 이번 미션의 목표

- [ ] 클래스를 통해 코드를 구조적으로 분리한다.
- [ ] 입력 / 검증 / 출력의 책임을 명확히 분리한다.
- [ ] 메서드가 한 가지 기능을 하도록 한다.
- [ ] App에서 입력과 출력을 분리하고, 핵심 로직을 독립시켜본다.
- [ ] 모듈화를 시도해본다.
- [ ] 상수를 정의하고 의미있는 이름을 부여하여 해당 값이 어떤 역할을 하는지 명확히 드러낸다.
- [ ] 예외 메시지를 한 곳에서 관리해본다.

---

## 기능 구현 목록

✅ 입력

- [x] 로또 구입 금액(readPurchaseAmount) 입력
- [x] 당첨 번호(readWinningNumbers) 입력
- [x] 보너스 번호(readbonusNumber) 입력

✅ 유효성 검증

- [x] 로또 구입 금액에 대한 유효성 검증(validatePurchaseAmount)

  - [x] 1000원 단위로 입력 받지 않은 경우 [ERROR] 처리 후 재시도

  - [x] 숫자가 아닌 문자로 입력받았을 경우 [ERROR] 처리 후 재시도

  - [x] 0 이하인 경우 [ERROR] 처리 후 재시도

  - [x] 빈 값을 입력했을 경우 [ERROR] 후 재시도

- [x] 당첨 번호에 대한 유효성 검증(validateWinningNumbers)

  - [x] 쉼표를 기준으로 구분하지 않은 경우 [ERROR] 처리 후 재시도

  - [x] 숫자가 아닌 문자로 입력받았을 경우 [ERROR] 처리 후 재시도

  - [x] 숫자가 1~45 이외인 경우 [ERROR] 처리 후 재시도

  - [x] 중복된 숫자가 있을 경우 [ERROR] 처리 후 재시도

  - [x] 빈 값 입력 시 [ERROR] 처리 후 재시도

  - [x] 6개 미만 또는 초과 입력 시 [ERROR] 처리 후 재시도

- [x] 보너스 번호에 대한 유효성 검증(validateBonusNumber)

  - [x] 숫자가 아닌 문자로 입력받았을 경우 [ERROR] 처리 후 재시도

  - [x] 숫자가 1~45 이외인 경우 [ERROR] 처리 후 재시도

  - [x] 당첨 번호와 중복될 경우 [ERROR] 처리 후 재시도

✅ 실행 로직

- [x] 로또 발행(LottoGenerator)

  - [x] 구입 금액에 따라 로또 개수(lottoCount) 계산
  - [x] 로또 번호를 저장할 배열(lottos) 생성
  - [x] 로또 개수만큼 반복하며 번호 생성
    - [x] 1~45 사이의 중복없는 숫자를 6개를 생성
    - [x] 번호 오름차순으로 정렬
    - [x] Lotto 인스턴스 생성하여 유효성 검증
    - [x] lottos에 생성된 번호 추가

- [x] 수익률 계산(calculateProfit)

  - [x] 각 등수별 당첨 금액과 일치한 횟수 곱함(totalPrize)
  - [x] (totalPrize / purchaseAmount) \* 100 하여 수익률 계산
  - [x] 수익률의 소수점 둘째 자리에서 반올림
  - [x] 수익률 출력

✅ 출력

- [x] 발행된 로또 개수(lottoCount)와 번호를 한 줄씩 출력
- [x] 당첨 결과 통계 출력(calculateWinningResult)
- [x] 수익률 출력 (calculateProfit)

---

## 프로그래밍 흐름

1.  로또 구입 금액(purchaseAmount)을 입력받는다.

2.  로또 구입 금액에 대한 유효성 검증(validatePurchaseAmount)을 한다.
    2-1. 1000원 단위로 입력 받지 않은 경우 [ERROR] 처리 후 재시도

    2-2. 숫자가 아닌 문자로 입력받았을 경우 [ERROR] 처리 후 재시도

    2-3. 0 이하인 경우 [ERROR] 처리 후 재시도

3.  발행한 로또 수량만큼 로또를 발행한다.(LottoGenerator)
    3-1. 로또 발행 개수(lottoCount)를 계산한다.

        3-1-1. 로또 구입 금액(purchaseAmount) / 1000으로 계산한다.

    3-2. 로또 번호를 담을 배열(lottos)을 선언한다.

    3-3. 로또 발행 개수만큼 반복하면서 로또를 발행한다.

        3-3-1. 1~45 사이의 중복없는 숫자를 6개를 생성한다.

        3-3-2. Lotto 인스턴스를 생성하여 유효성 검증을 한다.

        3-3-3. 번호를 오름차순으로 정렬한다.

4.  발행한 로또 수량(lottoCount)과 로또 번호 목록(lottos)를 출력한다.

5.  당첨 번호(winningNumbers)를 입력받는다.

6.  당첨 번호에 대한 유효성 검증(validateWinningNumbers)을 한다.
    6-1. 쉼표를 기준으로 구분하지 않은 경우 [ERROR] 처리 후 재시도

    6-2. 숫자가 아닌 문자로 입력받았을 경우 [ERROR] 처리 후 재시도

    6-3. 숫자가 1~45 이외인 경우 [ERROR] 처리 후 재시도

    6-4. 중복된 숫자가 있을 경우 [ERROR] 처리 후 재시도

    6-5. 빈 값 입력 시 [ERROR] 처리 후 재시도

    6-6. 6개 미만 또는 초과 입력 시 [ERROR] 처리 후 재시도

7.  보너스 번호(bonusNumber)를 입력받는다.

8.  보너스 번호에 대한 유효성 검증(validateBonusNumber)을 한다.
    8-1. 숫자가 아닌 문자로 입력받았을 경우 [ERROR] 처리 후 재시도

    8-2. 숫자가 1~45 이외인 경우 [ERROR] 처리 후 재시도

    8-3. 당첨 번호와 중복될 경우 [ERROR] 처리 후 재시도

9.  당첨 결과를 계산한다.(calculateWinningResult)

    9-1. 발행한 로또 수량만큼 반복한다.

        9-1-1. 로또 번호 배열(lottos)과 당첨 번호(winningNumbers)를 비교한다.

        9-1-2. 일치한 개수를 계산하고, 보너스 번호(bonusNumber) 일치 여부를 판단한다.

        9-1-3. 일치 개수를 확인하며 등수별 당첨 횟수를 누적한다.

    9-2. 당첨 결과 통계를 출력한다.

10. 수익률을 계산한다.(calculateProfit)
    10-1. 각 등수별 당첨 금액과 일치한 횟수를 곱한다.(totalPrize)

    10-2. (totalPrize / purchaseAmount) \* 100을 하여 수익률을 계산한다.

    10-3. 수익률의 소수점 둘째 자리까지 반올림한다.

    10-4. 수익률을 출력한다.

---

## 테스트 항목

- [x] 로또 구입 금액에 대한 유효성 검증(validatePurchaseAmount)

  - [x] 1000원 단위로 입력 받지 않은 경우

  - [x] 숫자가 아닌 문자로 입력받았을 경우

  - [x] 0 이하인 경우

- [x] 당첨 번호에 대한 유효성 검증(validateWinningNumbers)

  - [x] 쉼표를 기준으로 구분하지 않은 경우

  - [x] 숫자가 아닌 문자로 입력받았을 경우

  - [x] 숫자가 1~45 이외인 경우

  - [x] 중복된 숫자가 있을 경우

  - [x] 빈 값 입력 시

  - [x] 6개 미만 또는 초과 입력 시

- [x] 보너스 번호에 대한 유효성 검증(validateBonusNumber)

  - [x] 숫자가 아닌 문자로 입력받았을 경우

  - [x] 숫자가 1~45 이외인 경우

  - [x] 당첨 번호와 중복될 경우
